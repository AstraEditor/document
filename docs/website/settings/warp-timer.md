---
title: 循环计时器
---

# 循环计时器

循环计时器能让脚本检测自己是否卡在长时间的或无限的循环之中，并以降低运行帧率，避免因死循环的发生。这能解决大多数由于编写安排不当导致的崩溃问题，但会对性能产生显著影响，因此默认情况下仅在编辑器中启用。

[演示作品](https://turbowarp.org/446742201?stuck)在一个“运行而不刷新屏幕”的循环中包含了一个无限循环。启用循环计时器后，作品将以约 2 帧/秒的速度运行。不过，禁用了循环计时器，脚本将永远不会结束。

循环计时器此前曾被称为“卡死状态检查(Stuck Checking)”。