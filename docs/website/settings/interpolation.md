---
title: 插值
---

# 动画插值(帧)

插值技术通过在帧与帧之间对精灵的移动进行插值处理，使作品中的动画显得更加流畅。它能实现更高的帧率，同时不会影响脚本的运行速度。

以下情况不应启用动画插值：

- 3D 作品
- 光线追踪作品
- 画笔作品
- 运行巨慢的作品

插值不太可能让这些作品看起来更加流畅，因为插值无法模拟画笔的绘制，而且这样做还会降低绘制的性能。

插值操作可能会引入轻微的（最多 1/30 秒，也就是一帧）输入延迟。

在一些复杂的作品中，比如滚动平台类游戏，您可能会遇到一些图形方面的故障。